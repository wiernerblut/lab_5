pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo Building on main branch'
            }
        }
        stage('Test') {
            steps {
                sh 'echo Testing on dev branch'
            }
        }
    }
}
